export interface SignInRequest {
  email: string;
}
export interface recruitmentDetail {
  id: string;
  title: string;
  city: string;
  manager: string;
  callNumber: string;
  deadline: string;
  workTime: string;
  pay: string;
  career: string;
  detailTest: string;
  workSpace: string;
}
